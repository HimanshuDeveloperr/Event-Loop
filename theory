what is microtask queue?

so basically it is similar to our call back queue and works in a same way but has higher execution priority
as the functions which are inserted in this queue will get get executed first and then the function of call queue 
will be execute.